import React from "react";

const Header = (props) => {
	return (
		<div className="ui big secondary pointing menu">
			<h2 className="ui center aligned icon header">💣 Quize App 🔥</h2>
		</div>
	);
};

export default Header;
